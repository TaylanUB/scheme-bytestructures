- Bit-fields.  Possible with (rnrs arithmetic bitwise), and with
  non-portable APIs turning integers into bit arrays.

- Allow working with ports (or other open-ended byte sequences) as an
  alternative to bytevectors.

- A utility that reads C header files and spits out descriptors, in
  either object or source form, probably source.
